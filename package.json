{
  "name": "bookmcq-math-renderer",
  "version": "1.0.0",
  "description": "Pre-render math formulas for BookMCQ books using MathJax",
  "main": "pre-render-math.js",
  "scripts": {
    "render": "node pre-render-math.js"
  },
  "dependencies": {
    "mathjax-node": "^2.1.1"
  },
  "devDependencies": {
    "svgo": "^3.0.0"  // ← This will be added
  }
}
